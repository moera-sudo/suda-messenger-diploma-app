info:
  name: WS Connect
  type: websocket
  seq: 1

websocket:
  url: "ws://localhost:8080/api/v1/messenger/ws?token={{access_token}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Connected successfully", function() {
          expect(res.getStatus()).to.equal(101);
        });

settings:
  timeout: 0
