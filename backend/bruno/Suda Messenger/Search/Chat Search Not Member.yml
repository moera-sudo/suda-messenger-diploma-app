info:
  name: Chat Search Not Member
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}{{messenger_prefix}}/chats/00000000-0000-0000-0000-000000000000/search?q=test"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 403", function() {
          expect(res.getStatus()).to.equal(403);
        });

        test("Error is FORBIDDEN", function() {
          expect(res.getBody().error).to.equal("FORBIDDEN");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
