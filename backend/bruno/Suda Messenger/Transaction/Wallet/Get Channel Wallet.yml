info:
  name: Get Channel Wallet
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}{{transaction_prefix}}/wallet/channel/{{test_channel_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });
        test("Has address", function() {
          expect(res.getBody().address).to.be.a("string");
          expect(res.getBody().address).to.match(/^0x/);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
