info:
  name: Unhide Last Seen
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}{{messenger_prefix}}/last-seen-hidden/{{test_user_id_2}}"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
