info:
  name: Update Chat
  type: http
  seq: 10

http:
  method: PUT
  url: "{{base_url}}{{messenger_prefix}}/chats/{{test_group_chat_id}}"
  body:
    type: json
    data: |-
      {
        "name": "Updated Group Name",
        "description": "New description"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
