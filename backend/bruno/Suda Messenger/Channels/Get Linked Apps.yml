info:
  name: Get Linked Apps
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}{{messenger_prefix}}/channels/{{test_channel_id}}/apps"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });
        test("Has apps array", function() {
          expect(res.getBody().apps).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
