info:
  name: Get Subscribers
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}{{messenger_prefix}}/channels/{{test_channel_id}}/subscribers"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });
        test("Has subscribers array", function() {
          expect(res.getBody().subscribers).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
