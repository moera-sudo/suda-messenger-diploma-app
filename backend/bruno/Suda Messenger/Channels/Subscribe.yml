info:
  name: Subscribe
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}{{messenger_prefix}}/channels/{{test_channel_id}}/subscribe"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
