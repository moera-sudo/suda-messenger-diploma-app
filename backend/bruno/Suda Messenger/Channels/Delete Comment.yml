info:
  name: Delete Comment
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}{{messenger_prefix}}/channels/comments/{{test_comment_id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status is 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
